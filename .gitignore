# Ignore everything in data/ folder
data/**

# But keep  .tar.gz files in data/
!data/*.tar.gz

# Ignore auxiliary/ and validation/ folders in root
/auxiliary/
/validation/

#Ignore pycache folders
**/__pycache__

#Ignore log files
training/log.txt
sampling/log.txt
sampling/logs/

# card kept locally only (no workspace for this analysis)
sampling/cards/2102.10874.yaml

# scratch: files staged for manual deletion
_to_delete/

# kept locally, not published: maintainer scripts, the test harness and the
# fixtures and run logs that belong to them
sampling/tools/
sampling/tests/
sampling/harvest_logs/
sampling/parameters_test.yaml
sampling/cards/test-*.yaml
